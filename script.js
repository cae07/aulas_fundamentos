function greetings(name) {
  return `Olá ${name}, tenha um ótimo dia.`;
}

console.log(greetings('Caê'));

function sum(firstValue, secondVelue) {
  return firstValue + secondVelue;
};

console.log(`O resultado da soma é = ${sum(10, 17)}`);