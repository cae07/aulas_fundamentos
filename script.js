const isTrue = 3 + 5 === 7 ? true : false;

// informação ? isso é verdade : isso é mentira;

// console.log(isTrue);

const verifySum = (valueA, valueB) =>
  valueA + valueB === 7 ? 'isso é verdade' : 'isso é mentira';


console.log(verifySum(3, 5));