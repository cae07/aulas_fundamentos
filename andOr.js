const bebida = 'café';
const comida = 'pão na chapa';

const isBreakfast = bebida === 'café' && comida === 'ovos mexidos';
const isValid = bebida === 'leite' || comida === 'pão na chapa';

// console.log(isBreakfast);
// console.log(isValid);
